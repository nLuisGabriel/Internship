import { PlatesPipePipe } from './plates-pipe.pipe';

describe('PlatesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlatesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
